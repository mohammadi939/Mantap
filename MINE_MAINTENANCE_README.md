# 🏭 سیستم مدیریت تعمیرات معدن

## 📋 معرفی

این سیستم یک راه‌حل جامع برای مدیریت تعمیرات، تجهیزات و موجودی در معادن است که با Python و Flask توسعه یافته است.

## ✨ ویژگی‌های کلیدی

### 🔧 مدیریت تجهیزات
- ثبت و مدیریت تجهیزات معدن
- پیگیری وضعیت تجهیزات (فعال، در حال تعمیر، خراب، بازنشسته)
- برنامه‌ریزی تعمیرات پیشگیرانه
- مدیریت گارانتی و تاریخ خرید

### 🛠️ مدیریت تعمیرات
- برنامه‌ریزی کارهای تعمیرات
- تعیین اولویت (کم، متوسط، بالا، بحرانی)
- تخصیص تکنسین
- پیگیری هزینه‌ها و قطعات مصرفی

### 📦 مدیریت موجودی
- ثبت قطعات و لوازم
- هشدار موجودی کم
- مدیریت تامین‌کنندگان
- پیگیری مکان قطعات

### 📊 گزارش‌گیری
- آمار کلی تجهیزات
- گزارش‌های تعمیرات ماهانه/هفتگی
- تحلیل هزینه‌ها
- نمودارهای وضعیت

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.8 یا بالاتر
- pip (مدیر بسته‌های Python)

### مراحل نصب

1. **کلون کردن پروژه:**
```bash
git clone <repository-url>
cd mine-maintenance-system
```

2. **نصب وابستگی‌ها:**
```bash
pip install -r requirements.txt
```

3. **اجرای سیستم:**
```bash
python web_interface.py
```

4. **دسترسی به سیستم:**
- مرورگر را باز کنید
- به آدرس `http://localhost:5000` بروید

## 📁 ساختار پروژه

```
mine-maintenance-system/
├── mine_maintenance_system.py    # سیستم اصلی
├── web_interface.py              # رابط کاربری وب
├── requirements.txt              # وابستگی‌ها
├── templates/                    # قالب‌های HTML
│   ├── base.html
│   ├── dashboard.html
│   ├── equipment.html
│   └── add_equipment.html
└── mine_maintenance.db           # پایگاه داده SQLite
```

## 🎯 نحوه استفاده

### 1. داشبورد
- مشاهده آمار کلی
- وضعیت تجهیزات
- کارهای فوری
- دسترسی سریع

### 2. مدیریت تجهیزات
- افزودن تجهیز جدید
- ویرایش اطلاعات
- برنامه‌ریزی تعمیرات
- فیلتر و جستجو

### 3. مدیریت تعمیرات
- ایجاد کار تعمیرات
- تخصیص تکنسین
- پیگیری پیشرفت
- تکمیل کارها

### 4. مدیریت موجودی
- افزودن قطعات
- هشدار موجودی کم
- مدیریت تامین‌کنندگان

### 5. گزارش‌گیری
- گزارش‌های ماهانه
- آمار هزینه‌ها
- نمودارهای تحلیلی

## 🔧 تنظیمات

### تغییر پورت
```python
# در فایل web_interface.py
app.run(debug=True, host='0.0.0.0', port=8080)  # تغییر پورت به 8080
```

### تغییر پایگاه داده
```python
# در فایل web_interface.py
app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql://user:pass@localhost/mine_db'
```

## 📊 API Endpoints

### تجهیزات
- `GET /api/equipment/<id>` - دریافت اطلاعات تجهیز
- `POST /equipment/add` - افزودن تجهیز جدید

### تعمیرات
- `GET /maintenance` - لیست کارهای تعمیرات
- `POST /maintenance/add` - افزودن کار تعمیرات
- `POST /api/maintenance/complete/<id>` - تکمیل کار تعمیرات

### موجودی
- `GET /inventory` - لیست موجودی
- `POST /inventory/add` - افزودن قطعه

## 🛡️ امنیت

- احراز هویت کاربران
- رمزگذاری کلمات عبور
- کنترل دسترسی بر اساس نقش
- لاگ‌گیری فعالیت‌ها

## 🔄 پشتیبان‌گیری

```python
# پشتیبان‌گیری از پایگاه داده
import shutil
shutil.copy('mine_maintenance.db', 'backup/mine_maintenance_backup.db')
```

## 📈 توسعه آینده

- [ ] اپلیکیشن موبایل
- [ ] اتصال به سنسورهای IoT
- [ ] هوش مصنوعی برای پیش‌بینی خرابی
- [ ] اتصال به سیستم‌های ERP
- [ ] گزارش‌گیری پیشرفته
- [ ] اعلان‌های خودکار

## 🤝 مشارکت

برای مشارکت در توسعه این پروژه:

1. Fork کنید
2. شاخه جدید ایجاد کنید (`git checkout -b feature/amazing-feature`)
3. تغییرات را commit کنید (`git commit -m 'Add amazing feature'`)
4. Push کنید (`git push origin feature/amazing-feature`)
5. Pull Request ایجاد کنید

## 📞 پشتیبانی

برای پشتیبانی و گزارش باگ:
- ایمیل: support@mine-maintenance.com
- تیکت: GitHub Issues

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است. برای جزئیات بیشتر فایل LICENSE را مطالعه کنید.

---

**توسعه‌دهنده:** سیستم مدیریت تعمیرات معدن  
**نسخه:** 1.0.0  
**تاریخ:** 2024